import { PipeDateFormatPipe } from './pipe-date-format.pipe';

describe('PipeDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
