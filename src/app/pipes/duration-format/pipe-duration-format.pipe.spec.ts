import { PipeDurationFormatPipe } from './pipe-duration-format.pipe';

describe('PipeDurationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDurationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
